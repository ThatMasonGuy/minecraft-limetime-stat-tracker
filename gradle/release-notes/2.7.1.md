# Lifetime Stat Tracker 2.7.1

Validation refresh for Minecraft `1.20` through `26.2-pre-3`.

Choose the file that matches your Minecraft range:

- `1.20-1.20.4`
- `1.20.5-1.21.10`
- `1.21.11`
- `26.1-26.2-pre-3`

Highlights:

- Revalidated the published compatibility-group jars with the upgraded release
  pipeline.
- Added dedicated-server launch smoke testing to the release gate, so the
  optional server component is now checked before publishing.
- The server smoke test verifies the Fabric server entrypoint, `/lstserver`
  command registration, world identity resolution, and clean dedicated-server
  shutdown.
- Client-only installs, optional Fabric server installs, commands, and saved
  data formats are unchanged from `2.7.0`.
