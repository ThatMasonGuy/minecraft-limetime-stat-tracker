# Lifetime Stat Tracker 2.8.1

Shared Tempest Studios data folder with instance/profile namespacing for
Minecraft `1.20` through `26.2-pre-3`.

Choose the file that matches your Minecraft range:

- `1.20-1.20.4`
- `1.20.5-1.21.10`
- `1.21.11`
- `26.1-26.2-pre-3`

Highlights:

- Lifetime Stat Tracker now writes saved JSON data under the shared Tempest
  Studios app-data folder:
  - Windows: `%APPDATA%\TempestStudios\Lifetime-Stat-Tracker\`
  - macOS: `~/Library/Application Support/TempestStudios/Lifetime-Stat-Tracker/`
  - Linux: `$XDG_DATA_HOME/tempest-studios/lifetime-stat-tracker/` or
    `~/.local/share/tempest-studios/lifetime-stat-tracker/`
- Actual JSON files live under
  `instances/<instance>/profiles/<player profile>/` inside the shared root, so
  different game directories and different Minecraft accounts no longer merge
  lifetime totals or same-named local worlds.
- On first run, existing unnamespaced data is copied into the active
  instance/profile folder if that folder does not already contain tracker data.
  The migration checks unnamespaced shared-folder data first, then `2.8.0`
  app-data, then older launcher-local
  `.minecraft/config/lifetime-stat-tracker/` data.
- Each legacy source is auto-imported only once to avoid copying a mixed old
  dataset into every future profile namespace.
- Legacy data is left untouched as a safety backup.
